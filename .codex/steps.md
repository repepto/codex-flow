# Steps

This file contains executable pending steps for `run-steps`.

This file is user-maintained. Codex must not mutate this file during `run-steps`.

`run-steps` reads this file and executes the steps in order as an atomic chain.

Executable item grammar is defined in `.codex/commands.md`. In short, each item uses a second-level heading, one `Task:` label, task text, and an optional `---` separator.

Do not store examples in this file.

No pending steps.

Examples live in `.codex/run-step-examples.md`.
